import json


def process_data(data_str):
    # Convertir la cadena JSON a un objeto Python
    data = json.loads(data_str)

    # Aquí se pueden agregar más procesamientos si es necesario.
    return data
